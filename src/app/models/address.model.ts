export class Address {
    constructor(public id:string,public nickname: string, public add_line: string, public district: string,
                public state: string, public country: string, public pincode: number) {}
}